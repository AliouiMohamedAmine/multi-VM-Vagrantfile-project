# Multi-VM Vagrant Project

## Overview
This project demonstrates how to set up a multi-VM environment using Vagrant. It includes:
- A web server (Apache)
- A database server (MySQL)

## Prerequisites
- Vagrant

## Usage
1. Clone this repository:
   ```bash
   git clone https://github.com/AliouiMohamedAmine/multi-VM-Vagrant-project.git
